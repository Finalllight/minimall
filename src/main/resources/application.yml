spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mini_mall?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
  thymeleaf:
    cache: false
  sql:
    init:
      mode: always

server:
  port: 8080
